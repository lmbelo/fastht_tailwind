[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fastht_tailwind"
version = "1.0.0"
description = "Base package for FastHT using Tailwind."
authors = [
    { name = "Your Name", email = "your.email@example.com" },
]
dependencies = []